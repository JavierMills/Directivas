import { CustmoIfDirective } from './custmo-if.directive';

describe('CustmoIfDirective', () => {
  it('should create an instance', () => {
    const directive = new CustmoIfDirective();
    expect(directive).toBeTruthy();
  });
});
